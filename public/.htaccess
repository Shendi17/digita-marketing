<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /digita-marketing/
    
    # Redirection vers index.php si le fichier/dossier n'existe pas
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
